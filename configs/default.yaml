model:
  bart_model: "facebook/bart-large"
  dpr_encoder: "facebook/dpr-question_encoder-single-nq-base"
  
training:
  batch_size: 256
  max_length: 1024
  epochs: 3
  
retrieval:
  top_k: 100
  expansion_terms: 5
  prf_top_n: 10

paths:
  data_dir: "data"
  model_dir: "models"
  index_dir: "indexes"